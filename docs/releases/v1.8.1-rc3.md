# v1.8.1-rc3

`v1.8.1-rc3` is the reviewed successor to rc2. It keeps the
user-facing cache-prebuild module and the operator controls introduced in rc2,
while closing data-integrity and lifecycle defects found during release review.
It remains a development candidate and is deployed only to the isolated
Jiulongjiang candidate tree on machine 133 for acceptance. This is not an
authorization to modify stable production or the source data tree.

## Candidate changes

- Rejects a source ZIP that changes after capacity planning, binds the exact
  path/size/CRC entry set to the archive opened for extraction, and verifies
  the archive snapshot again before atomic publication.
- Replaces age-only extraction locks with a host/PID/UUID lease. A live local
  task cannot be reclaimed by age, stale recovery remains explicit, owner
  write failures clean up safely, and an old cleanup token cannot delete a new
  task's lock.
- Preserves terminal analysis/report results across status refresh and
  stop-versus-natural-completion races.
- Requires SHA-256 as well as byte count for strict report artifact binding.
- Prevents an RMS-only refresh from overwriting raw grouped time-history plots
  with synthetic zero-valued lines.
- Adds a non-disruptive Qt offscreen audit-build path for use while the desktop
  is occupied. Offscreen screenshots are explicitly marked non-native and
  cannot satisfy the formal release-package gate.
- Recomputes every frozen distribution file's path, byte count and SHA-256
  immediately before compression, and requires all embedded version records to
  match the requested release version.
- Removes the low-capability office PC from the shared storage-location
  profiles while retaining it in the operations inventory for light remote
  maintenance only.
- Makes raw CSV cache prebuild an optional, unchecked-by-default capability for
  every configured bridge. `jlj_daily_export`, `dated_folders` and
  `hongtang_period` are dispatched to their matching cache contracts; only
  configured analysis CSVs are eligible and source CSV/ZIP files are never
  moved or deleted.
- Restores the old MATLAB drag workflow as one two-line dialog: lower and upper
  horizontal bounds share one required time window, initially spanning the
  loaded preview, and the purple window boundaries can adjust that window. Exact entry,
  sampled deletion estimates, cancel and undo remain available.
- Adds two independent box-selection actions. A lower-side box takes the highest
  finite sample actually inside the box; an upper-side box takes the lowest.
  Equal-to-bound samples remain, the box boundary is never used as a threshold,
  and confirmation only updates the in-memory table until the operator saves.
- Binds manually loaded curve previews to the current bridge id, layered-config
  SHA-256, canonical data root and start/end dates. A legacy or mismatched
  preview fails closed instead of relying on a visual month check.

## Validation boundary

- The complete Python regression ran `597` tests successfully (`596` passed and
  `1` was conditionally skipped). MATLAB passed `675/675` with no failure or
  incomplete test.
- The final rebuilt workbench passed native Windows probing on the development
  machine: it
  owned both the foreground and keyboard-focus windows, exposed its native
  icon, reported 120 DPI with per-monitor awareness, and rendered at
  2018 x 1122 physical pixels. All 16 native screenshots passed their strict
  manifest gate.
- The rebuilt analysis Runner SHA-256 is
  `902dcd79956e9d091227d004cef47be6003a72325ee115f7b418d6ad31eb8531`;
  the workbench EXE SHA-256 is
  `8be37fcd576547a77d72b1d04135fe716bc56ac862a69ab32157640b636d8af9`.
  The release inventory closes `384/384` files, and the prepared Windows ZIP
  SHA-256 is
  `60358c2f643b82d144580bba4562f7ca5fd7ee642c98d5a1398e6c6246f4e650`.
- No rc3 build or test may write to the legacy production tree or source data.
  Authorized acceptance writes remain confined to the isolated
  `F:\Guanbing_v1.8.1-rc1` candidate tree on machine 133.

## Machine-133 acceptance status

- The reviewed rc3 archive is deployed under
  `F:\Guanbing_v1.8.1-rc1\app`. Its deployment receipt is
  `F:\Guanbing_v1.8.1-rc1\deployment_receipt_rc3_20260715_231823.json`, and
  the rollback tree is
  `F:\Guanbing_v1.8.1-rc1\app_before_rc3_20260715_231823`. Stable
  `F:\Guanbing` remains unchanged.
- The four-worker Jiulongjiang May cache run has completed successfully:
  `created=3764`, `reused=1561`, `eligible=5325`, `failed=0`, elapsed
  `6h50m27s`. Verified cache size at completion was `57.50 GiB` and F: free
  space was `539.33 GiB`.
- This completion is not the final Jiulongjiang acceptance. An immediate
  `force_rebuild=false` reuse rerun is currently validating all 5,325
  MAT/metadata pairs against a pre-run inventory. Its zero-write result, the 15
  applicable analysis modules, and the May report/Word-PDF page review remain
  pending.

## Post-package configuration correction

- A later source audit found Guanbing strain/crack groups and G05/G06 y-limits
  retained in the Jiulongjiang configuration. The working tree now removes
  those foreign references while preserving all 50 explicit strain points and
  22 explicit crack points.
- Post-fix regression passes Python `592 passed, 1 skipped` plus `180 subtests`
  and MATLAB `676/676`.
- This correction is newer than the immutable rc3 archive and therefore does
  not change the recorded ZIP, EXE, Runner or 384-file inventory hashes. The
  current isolated analysis uses a reviewed task-specific config snapshot; a
  later rebuilt candidate is required before promotion.
